<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goktech.shopping.mapper.TestMapper">
    <select id="getById" resultType="edu.sicau.shopping.domain.Tbclass">
        select
            id,name,numbers,size,createDate
        from
            class
        where
            id=#{id}
    </select>
</mapper>