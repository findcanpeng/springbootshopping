server:
  port: 8001
spring:
  application:
    name: spring-cloud-shopping
  datasource:
    url: jdbc:mysql:///crud?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: 548658
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: mapper/*Mapper.xml

eureka:
  client:
    service-url:
      defaultZone: http://root@127.0.0.1:8761/eureka
